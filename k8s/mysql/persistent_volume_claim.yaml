apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
  namespace: ehr-transcript-consolidation-service
spec:
  storageClassName: standard-rwo # GKE Autopilot default storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi