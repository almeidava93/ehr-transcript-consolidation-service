[project]
name = "ehr-transcript-consolidation-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mypy>=2.3.0",
    "openai-agents>=0.18.3",
    "pytest>=9.1.1",
    "ruff>=0.15.22",
]
